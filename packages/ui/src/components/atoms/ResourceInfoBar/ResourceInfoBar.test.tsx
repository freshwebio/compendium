import React from 'react'
import { create } from 'react-test-renderer'

import ResourceInfoBar from './ResourceInfoBar'

describe('ResourceInfoBar', () => {
  it('should render without any issues', () => {
    // Do something!
  })
})
