import styled from 'styled-components'

const CommitPanelWrapper = styled.div`
  position: absolute;
  top: 50%;
  right: 100px;
  transform: translateY(-50%);
  z-index: 3501;
`

export { CommitPanelWrapper }
